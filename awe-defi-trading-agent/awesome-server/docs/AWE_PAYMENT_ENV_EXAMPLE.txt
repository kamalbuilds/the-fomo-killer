# AWE Token Payment Configuration
# Add these environment variables to your .env file

# Base Chain RPC URL (required for AWE payments)
# You can use the public RPC or your own node
BASE_RPC_URL=https://mainnet.base.org

# Optional: Custom AWE/USD price 
# If not set, defaults to $0.1 per AWE
# AWE_USD_PRICE=0.1

# Optional: Custom gas price settings
# BASE_GAS_PRICE_GWEI=0.01

# Optional: Block confirmation settings
# AWE_PAYMENT_CONFIRMATIONS=1

# Optional: Payment expiration time (in minutes)
# AWE_PAYMENT_EXPIRY_MINUTES=60 